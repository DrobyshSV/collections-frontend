import React, { memo } from 'react';

interface CollectionEditPageProps {
  className?: string;
}

const CollectionEditPage = memo(({}: CollectionEditPageProps) => {
  return <p>CollectionEditPage</p>;
});

export default CollectionEditPage;
