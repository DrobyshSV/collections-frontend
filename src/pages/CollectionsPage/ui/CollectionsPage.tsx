import React, { memo } from 'react';

interface CollectionsPageProps {
  className?: string;
}

const CollectionsPage = memo(({}: CollectionsPageProps) => {
  return <p>CollectionsPage</p>;
});

export default CollectionsPage;
